<div class="chat-message{{#if className}} {{className}}{{/if}}">
  {{#each message.text}}
    <p class="chat-message__text">{{{this}}}<span class="chat-message__text-end"></span></p>
  {{/each}}
  <div class="chat-message__time">{{message.time}}</div>
</div>
